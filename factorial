/*

Task 11 - Create a recursive function that works out the factorial value of a number passed in an argument.

//WorkList

//Create a recursive function
//Create base case to stop the recursion
 */


function factorial(value)
{
  if(value === 0)
  {return 1}
  return value * factorial(value - 1);;
  }
